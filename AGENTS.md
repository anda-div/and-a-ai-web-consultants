# AGENTS.md

## このリポジトリの目的

- ウェブ解析・改善業務を7つの専門JOBに分け、Codexと共同で実行する。
- 利用者が指定したJOBだけを起動する。指定されていないJOBを勝手に追加しない。
- 実データと公開コードを分離し、クライアント情報をGitへ入れない。

## 作業開始時

1. 利用者が指定した `consultants/<JOB>/AGENTS.md` と `JOB.md` を読む。
2. `shared/QUALITY_POLICY.md` を読む。
3. 入力ファイル、対象期間、対象サイト、納品形式を確認する。
4. 不足情報があっても進められる部分は進め、不足箇所は「未検証」と明記する。

## 共通ルール

- 分析値は暗算せず、Python・GAS・表計算の再現可能な計算で検証する。
- 事実、解釈、仮説、提案を混同しない。
- 数値には出所、期間、分母、フィルタ条件を付ける。
- AIが推測した数値、事例、URL、引用を作らない。
- 実クライアント名、実URL、GA4/GTM ID、メール、認証情報をコミットしない。
- 既存ファイルを上書きせず、成果物は原則 `output/` に別名保存する。
- Pythonを変更したら関連テストと `python -m compileall` を実行する。
- 複数行のコードをシェルの引数として渡さない。ファイルに書いてから実行する。
  コード中の `>`（比較演算子、f文字列の桁揃え、正規表現）が出力リダイレクトとして
  解釈され、0バイトのファイルが黙って増える。`check_stray_files.py` で検出できる。
  同じ原因で `python` のようなコマンド名のファイルもできる。`where` がカレント
  フォルダを先に見るため、ツールが起動しなくなる。中身があっても起きるので、
  空ファイル検査だけでは足りない。
- GASを変更したら設定値がプレースホルダであることと、秘密情報がないことを確認する。
- 既存のGAS案件をPCからの直接取得へ移すときは `shared/PORTING_RUNBOOK.md` に従う。
  移す前の出力を必ず退避し、`shared/scripts/compare_xlsx.py` で全セルが一致するまで
  切り替えない。合計が合っていても、丸め・並び・表記が違えば下流の数値が変わる。
- APIをPCから直接呼ぶ構成では、着手前に `python shared/scripts/tls_env.py` で切り分ける。
  セキュリティソフトがHTTPSを検査していると、ブラウザは正常なのにPythonとgcloudだけが
  証明書エラーで止まる。対処は `shared/TLS_INSPECTION.md`。証明書を足すだけでは直らない
  ケースがあるので、エラー文を読み分ける。

## JOB間の受け渡し

- JOB 01〜04は、所見を `shared/schemas/finding.schema.json` に沿ったJSONまたはCSVで渡す。
- JOB 05は所見を統合し、課題IDと優先順位を確定する。
- JOB 06は課題IDを保持したまま改善案とワイヤーフレーム仕様へ変換する。
- JOB 07は課題ID・施策ID・根拠を維持し、レポートと効果検証を行う。

## 禁止事項

- 認証情報、APIキー、Cookie、個人情報を出力・コミットしない。
- 実装権限がない状態でGA4/GTM設定、サイト、外部サービスを変更しない。
- 計測不良の可能性を無視して分析を断定しない。
