# コントリビューション

IssueやPull Requestを歓迎します。

## 変更時の原則

- 実案件の名称・URL・ID・データを含めない。
- サンプルは `example.com` と架空の数値を使う。
- Python変更時は関連テストと `python -m compileall` を通す。
- READMEとコードの使い方が一致していることを確認する。
- 仕様変更は `CHANGELOG.md` に記録する。

## Pull Requestに含める内容

- 変更目的
- 対象JOB
- 実行したテスト
- 互換性への影響
- 機密情報を含まないことの確認
