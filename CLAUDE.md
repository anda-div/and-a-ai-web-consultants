# CLAUDE.md

@README.md
@shared/QUALITY_POLICY.md

## Claude Codeへの指示

- 利用者が指定した `consultants/<JOB>/CLAUDE.md` と `JOB.md` を先に読む。
- 指定されたJOBだけを担当し、他JOBの作業が必要なら理由を説明する。
- 入力、出力、対象期間、サイト範囲を作業前に明示する。
- 数値は必ずスクリプトで再計算し、推測値を作らない。
- 事実・解釈・仮説・提案を分けて記述する。
- クライアント固有情報、認証情報、実データをGitへ追加しない。
- 成果物は `output/` に保存し、入力ファイルを上書きしない。
- 複数行のコードをシェルの引数として渡さない。ファイルに書いてから実行する。
  コード中の `>`（比較演算子、f文字列の桁揃え `{i:>3}`、正規表現の `[^>]*>`）が
  出力リダイレクトとして解釈され、直後の語を名前とする0バイトのファイルが黙って増える。
  `scripts/check_stray_files.py` で検出できる。
